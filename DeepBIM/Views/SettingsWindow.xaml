<Window x:Class="DeepBIM.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DeepBIM.Views"
        mc:Ignorable="d"
        Icon="/DeepBIM;component/Resources/Icons/logo.png"
        Title="SettingsWindow" Height="230" Width="780">
    <!-- ===== THEME ===== -->
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Styles/Styles.xaml"/>
                <ResourceDictionary Source="../Resources/Styles/ComboboxStyle.xaml"/>
                <ResourceDictionary Source="../Resources/Styles/TextBlockStyle.xaml"/>
                <ResourceDictionary Source="../Resources/Styles/TextBoxStyle.xaml"/>
                <ResourceDictionary Source="../Resources/Styles/DataGridStyle.xaml"/>
                <ResourceDictionary Source="../Resources/Styles/CheckBoxStyle.xaml"/>
                <ResourceDictionary Source="../Resources/Styles/SliderStyle.xaml"/>
                <ResourceDictionary Source="../Resources/Styles/ColorSwatchStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Minimum Gap (mm):" Margin="0,0,0,5" FontWeight="SemiBold"/>

        <TextBox Grid.Row="1" x:Name="TextBoxMinGap" 
                 Margin="0,0,0,15" Height="30" 
                 Style="{StaticResource ModernTextBox}" 
                 Text="{Binding MinGap, UpdateSourceTrigger=PropertyChanged}" />

        <!-- ===== CONTROL BUTTONS ===== -->
        <StackPanel Grid.Row="2"
     Orientation="Horizontal"
     HorizontalAlignment="Right"
     Margin="0,24,0,0">

            <!-- Cancel button -->
            <Button Style="{StaticResource CancelButton}"
     Width="120"
     Margin="0,0,12,0"
     Click="CancelButton_Click">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <!-- Icon: dấu X -->
                    <TextBlock Text="✖" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Cancel" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- Apply button -->
            <Button Style="{StaticResource OkButton}"
     Width="120" Click="BtnSave_Click"
                    
     >
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <!-- Icon: dấu tick -->
                    <TextBlock Text="✔" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Apply" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Animation trigger -->
                <Button.Triggers>
                    <EventTrigger RoutedEvent="Button.Click">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                  To="0.7"
                                  Duration="{StaticResource AnimationDuration}"
                                  AutoReverse="True"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Button.Triggers>
            </Button>

        </StackPanel>
    </Grid>
</Window>
